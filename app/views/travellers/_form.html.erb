<%= form_for(@traveller) do |f| %>
  <% if @traveller.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@traveller.errors.count, "error") %> prohibited this traveller from being saved:</h2>

      <ul>
      <% @traveller.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit  %>
  </div>
<% end %>
